{% extends 'base.html.twig' %}

{% block title %}PubHouse{% endblock %}

{% block body %}
    <h1>PubHouse</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ pub_house.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ pub_house.name }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pub_house_index') }}">back to list</a>

    <a href="{{ path('app_pub_house_edit', {'id': pub_house.id}) }}">edit</a>

    {{ include('pub_house/_delete_form.html.twig') }}
{% endblock %}